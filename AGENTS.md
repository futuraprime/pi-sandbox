# Agent instructions

- TypeScript may be used in this repository, even if instructions elsewhere say otherwise.
- This repository is a downstream fork of `carderne/pi-sandbox`. Never push to its `upstream` remote or open pull requests against that upstream repository.
